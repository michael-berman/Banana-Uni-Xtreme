const database = {

  addScore(name, score){
    // let database = firebase.database();
    // var newScoreKey = firebase.database().ref().child('posts').push().key; for new key
    // let score =  key, score, username
    // firebase.database().ref('/users/').
  }

}

module.exports database;
